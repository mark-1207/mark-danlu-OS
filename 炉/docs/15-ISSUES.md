# 15 — 问题清单（Issues）

> 已知问题 + 解决方案 + 状态。
> 状态：🆕 新建 / 🚧 处理中 / ✅ 已解决 / ❌ 已废弃 / ⏸️ 搁置
> 维护：发现/解决时更新

---

## 🆕 I-001: 思想模型调用合理性判断规则待细化

**类型**：产品设计

**背景**：
- L5.2 思想模型应用评分要求判断"调用合理性"
- 但目前没有明确的判定规则
- 需要：是"为用而用"（堆术语）还是"真用到了"

**解决方案**：
- 在 ADR D-007 里细化合理性判断的 3 个维度
- 1. 识别：是否识别出文中用了 X 模型
- 2. 贴合：模型是解决问题还是堆术语
- 3. 解释：是否说明了怎么用

**状态**：🆕 待办

**优先级**：P1

---

## 🆕 I-002: 追问产出 → 锚点池的映射规则未定

**类型**：架构设计

**背景**：
- 苏格拉底追问产出 8 项 → 蓝图锚点池的映射规则
- 是 LLM 来分类，还是固定模板？

**倾向方案**：
- 固定模板：Q4 → contrarian，Q5 → case，Q6 → contrarian_candidates
- 简单可预测，不增加 LLM 调用

**状态**：🆕 待办

**优先级**：P1

---

## 🆕 I-003: 风格画像"必避免列表"如何动态更新

**类型**：架构设计

**背景**：
- 必避免列表需要动态学习
- 候选方案：
  - 用户主动标
  - diff 检测
  - 跑 N 篇后自动总结

**倾向方案**：
- 主动标 + diff 弱信号
- 频次：每篇 1 次

**状态**：🆕 待办

**优先级**：P1

---

## ✅ I-004: legacy 代码复制到 docs/legacy/ 已完成

**类型**：基础设施

**背景**：
- ContentForge + PRISM-OS 代码需要复制到 `docs/legacy/`
- 用 git subtree 还是手动 cp？

**解决方案**：
- 手动 cp（一次性快照，不需双向同步）
- 不带 `.git` 目录
- 剔除：node_modules / dist / output / __pycache__ / *.pyc / .env* / *.log / pnpm-lock.yaml
- 大小：CF 171M → 13M；PRISM-OS 36M → 25M
- 索引文档：`docs/legacy/README.md`

**状态**：✅ 已完成（2026-06-16）

**优先级**：P0

---

## ✅ I-005: 8 个 ADR 待写（已解决）

**类型**：文档

**背景**：
- D-001 ~ D-008 还没有正式 ADR 文档
- 决策已经敲定，ADR 是"决策当下详细说明"

**解决方案**：8 个 ADR 已全部写完

**状态**：✅ 已解决（2026-06-16）

**优先级**：P0

---

## ✅ I-006: .env.example 待创建

**类型**：基础设施

**背景**：
- 部署文档（08）提到 .env.example
- 但还没创建

**解决方案**：
- 创建 `.env.example`，列出所有需要的 API key / 飞书表格 / 模型配置
- 创建 `.env` 从兄弟项目同步真实 key
- 在 `src/lu/cli/run.py` 和 `src/lu/cli/embedding.py` 用 `python-dotenv` 自动加载项目根 `.env`

**状态**：✅ 已解决（2026-06-18）

**优先级**：P0

---

## ✅ I-007: pyproject.toml 待创建

**类型**：基础设施

**背景**：
- Python 项目初始化需要 pyproject.toml
- 用 uv 还是 poetry？

**倾向方案**：
- uv（更快、现代）

**解决方案**：
- `pyproject.toml` 已创建并维护（已添加 `numpy` / `python-dotenv` 依赖）

**状态**：✅ 已解决（2026-06-16）

**优先级**：P0

---

## ✅ I-008: v1 实施前需要确认 LLM Provider

**类型**：技术

**背景**：
- LLM 链：openai(mimo) → kimi → anthropic
- 哪个是 primary？费用/质量/速度？
- 需 mark 确认

**解决方案**：
- 2026-06-18 mark 确认：
  - 主要 LLM：mimo（OpenAI-compatible，https://api.xiaomimimo.com/v1）
  - fallback 1：kimi（Moonshot）
  - fallback 2：英伟达 NIM
- 已实现 `lu run --provider openai` 走 mimo → kimi → 英伟达 fallback 链
- `.env` / `.env.example` 已按此顺序配置

**状态**：✅ 已解决（2026-06-18）

**优先级**：P0

---

## 维护规范

- 新建问题：本文加 `🆕` 项
- 处理中：改为 `🚧`
- 解决：改为 `✅`，加解决日期 + 解决方式
- 废弃：改为 `❌`，加原因
- 搁置：改为 `⏸️`，加原因
- 优先级：P0/P1/P2
- 类型：基础设施 / 架构设计 / 产品设计 / 技术 / 文档 / 测试
