# 苏格拉底网关备选队列设计

## 目标

在苏格拉底网关（Phase 1）的方向选择阶段，允许用户将未选中的方向保存到"备选队列"，后续处理相似选题时自动作为额外候选出现。

## 交互流程

```
请选择一个更精确的方向：
1️⃣AI让某些职业消失
2️⃣AI时代新职业机会
3️⃣职业选择方法论转变（认知升级）

你的选择: 3

是否要将其他方向加入备选队列？
(输入编号，逗号分隔，如 1,2 或回车跳过): 1,2

✓ 已将2个方向加入备选队列
```

## 存储设计

### 飞书多维表格

复用爆款选题库同一张表，新增字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 状态 | 单选 | `备选` / `已使用` / `已过期` |
| 选题方向 | 文本 | 存储方向描述文本 |
| 来源 | 文本 | 标记来源，如 `socratic-2026-05-13` |

### 数据结构

```json
{
  "fields": {
    "标题": "AI让某些职业消失",
    "状态": "备选",
    "选题方向": "AI让某些职业消失",
    "来源": "socratic-2026-05-13",
    "备注": "用户输入：AI对职业的影响"
  }
}
```

## 相似度匹配

### 匹配流程

当用户输入新选题时：

1. 从飞书读取所有 `状态=备选` 的记录
2. 计算新选题与每条备选的 Embedding 相似度（cosine + Jaccard 加权）
3. 相似度 > 阈值（默认 0.7）的备选方向作为额外候选呈现
4. 用户确认后，将选中的备选方向加入本次候选列表

### 呈现格式

```
检测到2个相关备选方向：
  📌 AI让某些职业消失（相似度 0.82）
  📌 AI时代新职业机会（相似度 0.75）

是否将它们加入本次候选？(y/n):
```

### 关键参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| SIMILARITY_THRESHOLD | 0.7 | 相似度阈值，可配置 |
| BACKUP_EXPIRY_DAYS | 30 | 备选有效期，超期自动标记"已过期" |

## 实现要点

### 文件改动

- `scripts/socratic_gateway.py` — 添加备选队列交互逻辑
- `scripts/assassin.py` — 复用 `read_viral_library()` 读取备选，新增 `save_backup()` 写入
- `scripts/embedding.py` — 已有 `get_similarity()`，直接复用

### 新增函数

```python
# assassin.py
def save_backup(direction: str, source: str = "") -> bool:
    """将方向保存到飞书备选队列"""

def read_backup_queue() -> List[Dict]:
    """读取所有状态为"备选"的记录"""

def check_expired_backups(days: int = 30) -> int:
    """检查并标记过期备选"""

# socratic_gateway.py
def check_related_backups(topic: str, threshold: float = 0.7) -> List[Dict]:
    """检查相似备选方向，返回匹配结果"""
```

### 流程集成

在 `socratic_gateway()` 函数中：
1. 用户选择主方向后，调用 `save_backup()` 保存其他方向
2. 在 `run_prism_os()` 开始时，调用 `check_related_backups()` 检查相似备选
3. 如果有匹配，提示用户是否加入候选

## 验证标准

1. 选择方向后，能正确保存备选到飞书
2. 读取备选队列正常
3. 相似度匹配准确（测试用例：相似话题应匹配，无关话题不应匹配）
4. 过期机制正常工作
